import React from "react";

function NavBar() {
  // update the JSX being returned!
  NavBar = <nav>
    <a href = '#home'>Home</a>
    <a href = '#about'>About</a>
  </nav>
  return NavBar
}

export default NavBar;
